import LeanMining.NeuralGeometry.SubspaceOverlap

/-!
Paper-local compatibility wrapper.

The canonical implementation lives at
`LeanMining/NeuralGeometry/SubspaceOverlap.lean`. Keeping this file as an
import-only wrapper prevents the paper snapshot from drifting into a fork
with stale theorem and `sorry` counts.
-/
